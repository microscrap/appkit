<?php

namespace Microscrap\Bindings\AppKit\Enums;

/** AppKit `NSApplicationOcclusionState` (NSUInteger). Pass `::CASE->value` into ns_* ints. */
enum ApplicationOcclusionState: int
{
    case VISIBLE = 0;
}
