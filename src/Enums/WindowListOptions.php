<?php

namespace Microscrap\Bindings\AppKit\Enums;

/** AppKit `NSWindowListOptions` (NSInteger). Pass `::CASE->value` into ns_* ints. */
enum WindowListOptions: int
{
    case WINDOW_LIST_ORDERED_FRONT_TO_BACK = 0;
}
