<?php

namespace Microscrap\Bindings\AppKit\Enums;

/** AppKit `NSFontCollectionOptions` (NSUInteger). Pass `::CASE->value` into ns_* ints. */
enum FontCollectionOptions: int
{
    case FONT_COLLECTION_APPLICATION_ONLY_MASK = 1;
}
