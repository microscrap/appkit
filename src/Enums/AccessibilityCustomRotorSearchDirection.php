<?php

namespace Microscrap\Bindings\AppKit\Enums;

/** AppKit `NSAccessibilityCustomRotorSearchDirection` (NSInteger). Pass `::CASE->value` into ns_* ints. */
enum AccessibilityCustomRotorSearchDirection: int
{
    case PREVIOUS = 0;
    case NEXT = 1;
}
